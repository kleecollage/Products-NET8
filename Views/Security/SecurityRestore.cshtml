@model Web.Models.Usuario;
@{
  ViewData["Title"] = "Security";
  string FlashClass = (string) TempData["FlashClass"];
  string FlashMessage = (string) TempData["FlashMessage"];
}

<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Home</a></li>
    <li class="breadcrumb-item"><a asp-controller="Security" asp-action="Index">Security</a></li>
    <li class="breadcrumb-item"><a asp-controller="Security" asp-action="SecurityLogin">Login</a></li>
    <li class="breadcrumb-item active" aria-current="page">Restore</li>
  </ol>
</nav>

<h2 class="text-center">Restore Password</h2>

@if (FlashClass != null)
{
  <div class="alert alert-@FlashClass alert-dismissible fade show" role="alert">
    @FlashMessage
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
}

<div class="row">
    <form asp-controller="Security" asp-action="SecurityRestore" method="post">
      <div asp-validation-summary="All" class="text-danger"></div>
      <input type="hidden" asp-for="Nombre" value="empty">
      <input type="hidden" asp-for="Password" value="Password123*">

      <div class="form-group">
        <label asp-for="Correo" class="control-label"></label>
        <input asp-for="Correo" type="email" placeholder="Email" class="form-control" value="j.doe@mail.com">
      </div>

      <button type="submit" class="btn btn-primary mt-3"><i class="fas fa-plus"></i> Send </button>
    </form>
</div>
<hr />
<div class="row">
  <p>
    <a href="@Url.Action("SecurityRegister", "Security")"> Don't have an account yet? </a>
  </p>
</div>